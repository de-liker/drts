name: test

on:
  push:
    branches-ignore:
      - "main"
    paths:
      - ".github/workflows/test.yml"
      - "**"

jobs:

  test:
    name: test
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ./

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'

      - run: |
          npm ci

      - run: |
          npm run test
